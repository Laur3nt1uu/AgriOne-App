{
  "name": "agrione-serial-bridge",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Reads JSON lines from Arduino over Serial and forwards them to AgriOne /api/iot/ingest",
  "dependencies": {
    "serialport": "^12.0.0",
    "@serialport/parser-readline": "^12.0.0"
  }
}
