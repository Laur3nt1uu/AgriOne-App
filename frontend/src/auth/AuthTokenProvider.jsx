// AuthTokenProvider - placeholder for future external auth provider integration
// Currently unused since AgriOne uses custom JWT auth.
export function AuthTokenProvider({ children }) {
  return children;
}
